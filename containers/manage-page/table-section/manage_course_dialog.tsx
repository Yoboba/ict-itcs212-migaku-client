import React from "react";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";

type ManageCourseDialogProps = {
  title: string;
  description: string;
};

const ManageCourseDialog = ({
  title,
  description,
}: ManageCourseDialogProps) => {
  return <div>ManageCourseDialog</div>;
};

export default ManageCourseDialog;
